import { GroupOrdersPipe } from './group-orders.pipe';

describe('GroupOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
